<view>
  <z-navigation type="dark" icon="back" navigation-bar-color="{{ navigationBarColor }}" list="{{ list }}" scroll="{{ scroll }}" selected-id="{{ selectedId }}" bindtabchange="onTabsChange"></z-navigation>
</view>
