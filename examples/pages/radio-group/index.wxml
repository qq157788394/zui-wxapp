<view class="demo-page">
  <z-radio-group type="primary" inline list="{{ list }}" selected="{{ selected }}" bind:change="bindchange"></z-radio-group>
  <z-radio-group type="primary" list="{{ list }}" selected="{{ selected }}" bind:change="bindchange"></z-radio-group>
</view>
